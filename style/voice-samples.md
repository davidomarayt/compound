# Voice samples

Paste 20-30 sentences written in your own voice below, one per line. They are injected into the
drafting prompt as style examples. Lines starting with `#` and blank lines are ignored.

Tips: include a few openers, a few "here's the catch" lines, and a few sign-offs, so the model
sees how you start, warn, and finish.


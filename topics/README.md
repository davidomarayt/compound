# Topic bank (optional)

Put one topic per line in `health.md`, `wealth.md` or `happiness.md` here. The scheduled writer takes
the first line that has not been used yet (matched against existing item titles) before asking Claude
to propose a topic. Lines starting with `#` and blank lines are ignored. Delete or keep used lines as
you like; used ones are skipped automatically.
